<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Mevidence', root_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="<%= doctors_path %>" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"> 醫師一覽 <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to '所有科別', doctors_path %></li>
            <% Setting.all_cats.each do |c| %>
              <li><%= link_to c, doctors_path(:name => c) %></li>
            <% end %>
          </ul>
        </li>
        <li><%= link_to '醫院一覽', hospitals_path %></li>
      </ul>
    </div>
  </div>
</nav>
